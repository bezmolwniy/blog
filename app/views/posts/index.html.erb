<div class="col-lg-4 col-lg-offset-4">
	<h1>Timur's blog</h1>
	<hr>
	<% @posts.each do |post| %>
		<p>
			<%= post.created_at.strftime("%d/%m/%Y %H:%M") %>
			<%= link_to post.title, post %>
		</p>
	<% end %>
</div>